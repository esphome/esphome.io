---
import Default from "@astrojs/starlight/components/PageTitle.astro";

const hideTitle = Astro.url.pathname.startsWith("/starter-kit");
---

{!hideTitle ? <Default {...Astro.props} /> : <div class="no-page-title" />}
