---
description: "Changelog for ESPHome 2021.12.0."
title: "ESPHome 2021.12.0 - 11th December 2021"
pagefind: false
slug: "changelog/2021.12.0"
---

import PR from "@components/PR.astro";
import GHUser from "@components/GHUser.astro";


- [Button Core](/components/button/index/)
- [Template Button](/components/button/template/)
- [Restart Button](/components/button/restart/)
- [BLE Binary Output](/components/output/ble_client/)
- [Tuya Text Sensor](/components/text_sensor/tuya/)
- [SDP800 Series](/components/sensor/sdp3x/)


## State of the Open Smart Home

Today is the [State of the Open Smart Home](https://www.home-assistant.io/state-of-the-open-home/) hosted by Nabu
Casa, Home Assistant & ESPHome and we'll be joined by our friends from WLED, Z-Wave JS, Stanford's OVAL lab and
Northeastern University to talk about our work on making this vision a reality.

Where: YouTube

When: Today, Saturday, December 11, at 11am PST / 8pm CET

{/* markdownlint-disable-next-line MD033 */}
<iframe width="560" height="315" src="https://www.youtube-nocookie.com/embed/6ZMXE5PXPqU"
        title="YouTube video player" frameborder="0"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
        allowfullscreen>
</iframe>

## Buttons

This release brings [buttons](/components/button/index/) from Home Assistant. Many times there have
been questions on how to simply trigger automations from the Home Assistant frontend with a button so here you go.
Here is a very simple example:

```yaml
button:
  - platform: template
    name: Test Button
    on_press:
      then:
        - logger.log: "The button was pressed!!!"
```

## Multiple WiFi networks with static IP

This release has a fix that allows using multiple networks each with their own static IP address configuration.
When using these options, you **must** also set `wifi` -> `use_address` when installing new firmware as ESPHome
cannot know which IP address to use automatically.

## The Dashboard and secrets

The ESPHome Dashboard will now save the wifi credentials as secrets (`wifi_ssid` / `wifi_password`  ) for future use.
If you are already using these secrets, then the dashboard will not ask for credentials and will configure using
them for you.

The dashboard also now can view the logs straight from a serial connected device via the web browser. It will
show up as an option when choosing the method to view them.

## NEC remote protocol

In this version, the order of transferring bits was corrected from MSB to LSB in accordance with the NEC standard.
Therefore, if the configuration file has come from an earlier version of ESPhome, it is necessary to reverse
the order of the address and command bits when moving to 2021.12 or above.
For example, address: `0x84ED`, command: `0x13EC` becomes `0xB721` and `0x37C8` respectively.

## Tuya Covers

<PR number={2637} /> adds new functionality to the Tuya cover component, but at the same time
removes the "soft" inversion of the direction. Instead, it seems that most if not all tuya covers have a "hidden"
datapoint that sets the direction on the secondary MCU. See the [documentation](/components/cover/tuya/)
for the new configuration options.

{/* markdownlint-disable MD013 */}

## Release 2021.12.1 - December 15

- Set text sensor state property to filter output <PR number={2893} /> by <GHUser name="oxan" />
- Allow button POST on press from web server <PR number={2913} /> by <GHUser name="wilberforce" />

## Release 2021.12.2 - December 21

- Don't disable idle task WDT when it's not enabled <PR number={2856} /> by <GHUser name="oxan" />
- Fix MQTT button press action <PR number={2917} /> by <GHUser name="oxan" />

## Release 2021.12.3 - December 30

- Require arduino in webserver for better validation <PR number={2941} /> by <GHUser name="jesserockz" />
- Only allow internal pins for dht sensor <PR number={2940} /> by <GHUser name="jesserockz" />
- Workaround installing as editable package not working <PR number={2936} /> by <GHUser name="jesserockz" />
- Remove -e for hassio images <PR number={2964} /> by <GHUser name="jesserockz" />

## Full list of changes

### New Features

- pmsx003: add support for new PMS5003S device <PR number={2710} /> by <GHUser name="NeoAcheron" /> (new-feature)
- Add climate on_state trigger <PR number={2707} /> by <GHUser name="dudanov" /> (new-feature)
- Add greeyac protocol to IR Climate / HeatpumpIR <PR number={2694} /> by <GHUser name="cmroche" /> (new-feature)
- Provide an option to select unique_id generator <PR number={2701} /> by <GHUser name="kbialek" /> (new-feature)
- Ignore secrets yaml on command line <PR number={2715} /> by <GHUser name="cvwillegen" /> (new-feature)
- Add max_telegram_length option to dsmr <PR number={2674} /> by <GHUser name="mmakaay" /> (new-feature)
- Relax the icon validator to allow non-mdi icons <PR number={2764} /> by <GHUser name="paulmonigatti" /> (new-feature)
- Add support for P1 Data Request pin control <PR number={2676} /> by <GHUser name="mmakaay" /> (new-feature)
- Add support for sdp8xx <PR number={2779} /> by <GHUser name="martgras" /> (new-feature)
- Add map filter for text sensors <PR number={2761} /> by <GHUser name="oxan" /> (new-feature)
- Optionally show internal components on the web server <PR number={2627} /> by <GHUser name="mechanarchy" /> (new-feature)
- cse7766: add energy sensor <PR number={2822} /> by <GHUser name="ianchi" /> (new-feature)
- Allow Git credentials to be loaded from secrets <PR number={2825} /> by <GHUser name="mechanarchy" /> (new-feature)
- Expand uart invert feature to ESP8266 <PR number={1727} /> by <GHUser name="Lewn" /> (new-feature)

### New Components

- Add ble_client binary_output <PR number={2200} /> by <GHUser name="tekmaven" /> (new-integration)
- Add support for button entities <PR number={2824} /> by <GHUser name="jesserockz" /> (new-integration)

### Breaking Changes

- Fix NEC protocol implementation <PR number={2534} /> by <GHUser name="dudanov" /> (breaking-change)
- Tuya Cover improvements <PR number={2637} /> by <GHUser name="pauln" /> (breaking-change)

### Beta Changes

- Feed watchdog during OTA <PR number={2852} /> by <GHUser name="oxan" />
- Bump esphome-dashboard to 20211206.0 <PR number={2870} /> by <GHUser name="jesserockz" />
- tlc59208f : fix compilation error <PR number={2867} /> by <GHUser name="martgras" />
- ADC: Turn verbose the debugging "got voltage" <PR number={2863} /> by <GHUser name="CarlosGS" />
- SPS30 : fix i2c read size <PR number={2866} /> by <GHUser name="martgras" />
- Fix MCP23x17 not disabling pullup after config change <PR number={2855} /> by <GHUser name="oxan" />
- Ignore already stopped dhcp for ethernet <PR number={2862} /> by <GHUser name="madron" />
- Add endpoint to fetch secrets keys <PR number={2873} /> by <GHUser name="jesserockz" />
- Adopt using wifi secrets that should exist at this point <PR number={2874} /> by <GHUser name="jesserockz" />
- Allow wizard to specify secrets <PR number={2875} /> by <GHUser name="jesserockz" />
- Feed watchdog when no component loops <PR number={2857} /> by <GHUser name="oxan" />
- Bump esphome-dashboard to 20211207.0 <PR number={2877} /> by <GHUser name="jesserockz" />
- Feed watchdog while setting up OTA <PR number={2876} /> by <GHUser name="CarlosGS" />
- Use new platform component config blocks for wizard <PR number={2885} /> by <GHUser name="jesserockz" />
- Bump esphome-dashboard to 20211208.0 <PR number={2887} /> by <GHUser name="jesserockz" />
- Fix published state for modbus number <PR number={2894} /> by <GHUser name="jesserockz" />
- Modbus number/output use write single <PR number={2896} /> by <GHUser name="glmnet" />
- Fix for two points setting when fan_only_cooling is disabled <PR number={2903} /> by <GHUser name="kbx81" />
- Bump esphome-dashboard to 20211211.0 <PR number={2904} /> by <GHUser name="jesserockz" />

### All changes

- Defines tidy <PR number={2696} /> by <GHUser name="jesserockz" />
- pmsx003: add support for new PMS5003S device <PR number={2710} /> by <GHUser name="NeoAcheron" /> (new-feature)
- Add climate on_state trigger <PR number={2707} /> by <GHUser name="dudanov" /> (new-feature)
- Add greeyac protocol to IR Climate / HeatpumpIR <PR number={2694} /> by <GHUser name="cmroche" /> (new-feature)
- Fix rom/rtc.h deprecation compile warning for debug component <PR number={2520} /> by <GHUser name="mmakaay" />
- Drop unused constants from const.py <PR number={2718} /> by <GHUser name="oxan" />
- Install test requirements in lint Docker image <PR number={2719} /> by <GHUser name="oxan" />
- Clean-up MAC address helpers <PR number={2713} /> by <GHUser name="oxan" />
- Provide an option to select unique_id generator <PR number={2701} /> by <GHUser name="kbialek" /> (new-feature)
- Ignore secrets yaml on command line <PR number={2715} /> by <GHUser name="cvwillegen" /> (new-feature)
- Allow for subsecond sampling of hmc5883l <PR number={2735} /> by <GHUser name="jaharkes" />
- Change log level from DEBUG to INFO for sniffing services of `rf_bridge` <PR number={2736} /> by <GHUser name="nagyrobi" />
- Add ble_client binary_output <PR number={2200} /> by <GHUser name="tekmaven" /> (new-integration)
- Remove duplicated const data in esp8266 boards <PR number={2740} /> by <GHUser name="jesserockz" />
- Move to use improv lib from platformio <PR number={2741} /> by <GHUser name="jesserockz" />
- Add max_telegram_length option to dsmr <PR number={2674} /> by <GHUser name="mmakaay" /> (new-feature)
- Changed LUT for DKE epaper on ttgo t5 2.13inch to improve partial rfresh <PR number={2475} /> by <GHUser name="spattinson" />
- Fix NEC protocol implementation <PR number={2534} /> by <GHUser name="dudanov" /> (breaking-change)
- Remove arduino dependency from hm3301 <PR number={2745} /> by <GHUser name="martgras" />
- Add retry handler <PR number={2721} /> by <GHUser name="martgras" />
- Introduce str_snprintf helper function <PR number={2780} /> by <GHUser name="oxan" />
- Prettier date time display after time sync <PR number={2778} /> by <GHUser name="cvwillegen" />
- Fix distorted gif frames when resizing <PR number={2774} /> by <GHUser name="davet2001" />
- fixed wrong setup-usage tc9548a <PR number={2766} /> by <GHUser name="andreashergert1984" />
- Relax the icon validator to allow non-mdi icons <PR number={2764} /> by <GHUser name="paulmonigatti" /> (new-feature)
- Bump black from 21.10b0 to 21.11b1 <PR number={2760} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Update aht10.cpp to address issue #1635 <PR number={2675} /> by <GHUser name="krunkel" />
- Add support for P1 Data Request pin control <PR number={2676} /> by <GHUser name="mmakaay" /> (new-feature)
- Allow specifying the dashboard bind address <PR number={2787} /> by <GHUser name="jesserockz" />
- remove LEDC_HIGH_SPEED_MODE for C3, S2, S3 <PR number={2791} /> by <GHUser name="martgras" />
- Fix LEDC resolution calculation on ESP32-C3/S2/S3 <PR number={2794} /> by <GHUser name="rsumner" />
- Add support for sdp8xx <PR number={2779} /> by <GHUser name="martgras" /> (new-feature)
- Allow empty UART debug: option, logging in hex format by default <PR number={2771} /> by <GHUser name="mmakaay" />
- Bump pylint from 2.11.1 to 2.12.1 <PR number={2798} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Fix CI cache key for test3.yaml compile <PR number={2757} /> by <GHUser name="oxan" />
- Drop obsolete comment from CI workflow file <PR number={2758} /> by <GHUser name="oxan" />
- Cache virtualenv instead of pip cache between CI runs <PR number={2759} /> by <GHUser name="oxan" />
- Add map filter for text sensors <PR number={2761} /> by <GHUser name="oxan" /> (new-feature)
- Only match GCC warnings from ESPHome source files in CI <PR number={2756} /> by <GHUser name="oxan" />
- Correct constant for dynamic I2S bus in NeoPixelBus <PR number={2797} /> by <GHUser name="oxan" />
- Consistently format errors in CI scripts <PR number={2762} /> by <GHUser name="oxan" />
- Cancel previous workflows for PRs and branches <PR number={2800} /> by <GHUser name="jesserockz" />
- total_daily_energy: allow to disable restore mode <PR number={2795} /> by <GHUser name="ianchi" />
- Modbus_controller: Add custom command. <PR number={2680} /> by <GHUser name="martgras" />
- Fix usage of deprecated climate method in anova <PR number={2801} /> by <GHUser name="oxan" />
- Make clang-tidy suggest stdint.h int types <PR number={2820} /> by <GHUser name="oxan" />
- Fixed data type inside fast_random_8() routine <PR number={2818} /> by <GHUser name="anatoly-savchenkov" />
- Improve DSMR read timeout handling <PR number={2699} /> by <GHUser name="mmakaay" />
- Optionally show internal components on the web server <PR number={2627} /> by <GHUser name="mechanarchy" /> (new-feature)
- Fix custom mode_state_topic <PR number={2827} /> by <GHUser name="definitio" />
- Add support for button entities <PR number={2824} /> by <GHUser name="jesserockz" /> (new-integration)
- Tuya text_sensor and raw data usage <PR number={1812} /> by <GHUser name="dentra" />
- Fix 8266 SPI Clock Polarity Setting <PR number={2836} /> by <GHUser name="kbx81" />
- Fix too-broad matcher for custom CI script <PR number={2829} /> by <GHUser name="oxan" />
- cse7766: add energy sensor <PR number={2822} /> by <GHUser name="ianchi" /> (new-feature)
- Burst read for BME280, to reduce spurious spikes <PR number={2809} /> by <GHUser name="CarlosGS" />
- Bump aioesphomeapi from 10.2.0 to 10.6.0 <PR number={2840} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Button device class <PR number={2835} /> by <GHUser name="jesserockz" />
- Implement unit_of_measurement for number component <PR number={2804} /> by <GHUser name="puuu" />
- Number mode <PR number={2838} /> by <GHUser name="jesserockz" />
- Bump esphome-dashboard to 20211201.0 <PR number={2842} /> by <GHUser name="jesserockz" />
- Allow Git credentials to be loaded from secrets <PR number={2825} /> by <GHUser name="mechanarchy" /> (new-feature)
- Fix compile warning in Tuya automations <PR number={2837} /> by <GHUser name="oxan" />
- Add problem matcher for Python formatting errors <PR number={2833} /> by <GHUser name="oxan" />
- Fix CI check for Windows line endings <PR number={2831} /> by <GHUser name="oxan" />
- Don't enable namespace comment clang-tidy check twice <PR number={2830} /> by <GHUser name="oxan" />
- ESP32 Deep Sleep: correct level value <PR number={2812} /> by <GHUser name="jhamhader" />
- Fix OTA updates on esp8266 by declaring arch_get_cpu_cycle_count IRAM_ATTR and HOT <PR number={2843} /> by <GHUser name="Doridian" />
- Fix compilation using subprocesses <PR number={2834} /> by <GHUser name="oxan" />
- Expand uart invert feature to ESP8266 <PR number={1727} /> by <GHUser name="Lewn" /> (new-feature)
- Support setting manual_ip under networks option <PR number={2839} /> by <GHUser name="oxan" />
- Enable a bunch of clang-tidy checks <PR number={2149} /> by <GHUser name="oxan" />
- Set ESP32 watchdog to loop task <PR number={2846} /> by <GHUser name="oxan" />
- Tuya Cover improvements <PR number={2637} /> by <GHUser name="pauln" /> (breaking-change)
- Fix wifi not working with manual_ip using esp-idf <PR number={2849} /> by <GHUser name="Maelstrom96" />
- modbus_controller: bugfix: enable overriding calculated register size <PR number={2845} /> by <GHUser name="martgras" />
- Feed watchdog during OTA <PR number={2852} /> by <GHUser name="oxan" />
- Bump esphome-dashboard to 20211206.0 <PR number={2870} /> by <GHUser name="jesserockz" />
- tlc59208f : fix compilation error <PR number={2867} /> by <GHUser name="martgras" />
- ADC: Turn verbose the debugging "got voltage" <PR number={2863} /> by <GHUser name="CarlosGS" />
- SPS30 : fix i2c read size <PR number={2866} /> by <GHUser name="martgras" />
- Fix MCP23x17 not disabling pullup after config change <PR number={2855} /> by <GHUser name="oxan" />
- Ignore already stopped dhcp for ethernet <PR number={2862} /> by <GHUser name="madron" />
- Add endpoint to fetch secrets keys <PR number={2873} /> by <GHUser name="jesserockz" />
- Adopt using wifi secrets that should exist at this point <PR number={2874} /> by <GHUser name="jesserockz" />
- Allow wizard to specify secrets <PR number={2875} /> by <GHUser name="jesserockz" />
- Feed watchdog when no component loops <PR number={2857} /> by <GHUser name="oxan" />
- Bump esphome-dashboard to 20211207.0 <PR number={2877} /> by <GHUser name="jesserockz" />
- Feed watchdog while setting up OTA <PR number={2876} /> by <GHUser name="CarlosGS" />
- Use new platform component config blocks for wizard <PR number={2885} /> by <GHUser name="jesserockz" />
- Bump esphome-dashboard to 20211208.0 <PR number={2887} /> by <GHUser name="jesserockz" />
- Fix published state for modbus number <PR number={2894} /> by <GHUser name="jesserockz" />
- Modbus number/output use write single <PR number={2896} /> by <GHUser name="glmnet" />
- Fix for two points setting when fan_only_cooling is disabled <PR number={2903} /> by <GHUser name="kbx81" />
- Bump esphome-dashboard to 20211211.0 <PR number={2904} /> by <GHUser name="jesserockz" />

{/* markdownlint-enable MD013 */}

## All Changelogs


