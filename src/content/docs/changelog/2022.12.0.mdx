---
description: "Changelog for ESPHome 2022.12.0."
title: "ESPHome 2022.12.0 - 14th December 2022"
pagefind: false
slug: "changelog/2022.12.0"
---

import PR from "@components/PR.astro";
import GHUser from "@components/GHUser.astro";

- [ESP32](/components/esp32/)
- [Bluetooth Proxy](/components/bluetooth_proxy/)
- [SN74HC165 I/O Expander](/components/sn74hc165/)

The festive holidays are coming early and bring you an ESPHome release 1 week earlier than normal.
This release does not bring a bunch of new components like most releases, but instead the focus
has been on upgrading the core Arduino and ESP-IDF versions used for the ESP32 based devices.

## Ethernet

Because these core upgrade have aligned the version of ESP-IDF under the hood, `ethernet`
is now able to be used when specifying ESP-IDF as your framework of choice.

## Bluetooth Proxy

The Bluetooth Proxy has also had a whole lot of work done by <GHUser name="bdraco" /> under the hood to
improve the stability, speed and reliability between Home Assistant and the bluetooth devices
that get proxied through. Along with some of these bluetooth changes is a change to the
underlying flash partition table that ESPHome uses. OTA will work, but to fully take advantage
of the performance increases for bluetooth, it is best to at least one serial flash with ESPHome
2022.12.0 or later.

## Container images

> [!NOTE]
> This breaking change only affects you if you use docker containers directly and specify the suffix
> in your image.

2022.12.6 changes the way the container images are built and published. This is due to something
breaking somewhere that we could not explain. It has to do with the `docker manifest` command
which has been around for a while but always been marked as `experimental` so we cannot really
complain.

From now on there will be no builds with tags for the `-armv7` / `-amd64` / `aarch64` suffixes,
but the main images (`ghcr.io/esphome/esphome:<version>`  ) will be multi-arch capable and work on the
3 supported architectures.

{/* markdownlint-disable MD013 */}

## Release 2022.12.1 - December 16

- Support non-multiarch toolchains on 32-bit ARM <PR number={4191} /> by <GHUser name="agners" />
- Mark ESP32-S2 as not having Bluetooth <PR number={4194} /> by <GHUser name="jesserockz" />
- Fix i2s_audio media_player compiling for esp32-s2 <PR number={4195} /> by <GHUser name="jesserockz" />

## Release 2022.12.2 - December 20

- Fix import_full_config for adoption configs <PR number={4197} /> by <GHUser name="jesserockz" />
- Handle zero padding anywhere in the combined adv data <PR number={4208} /> by <GHUser name="bdraco" />

## Release 2022.12.3 - December 20

- Fix ESP32 GPIO when using INPUT PULLUP mode <PR number={4213} /> by <GHUser name="jesserockz" />

## Release 2022.12.4 - January 20

- Fix gpio pin mode for ISR pins <PR number={4216} /> by <GHUser name="jesserockz" />
- Always send the MTU request for BLE v3 cached connections <PR number={4322} /> by <GHUser name="bdraco" />

## Release 2022.12.5 - January 23

- Bump nano version in lint docker image <PR number={4218} /> by <GHUser name="jesserockz" />

## Release 2022.12.6 - January 24

- Move from docker manifest command to buildx with platforms <PR number={4320} /> by <GHUser name="jesserockz" /> (breaking-change)

## Release 2022.12.7 - January 26

- Add "content" to deploy-ha-addon-repo dispatch <PR number={4349} /> by <GHUser name="ludeeus" />
- fix stepper jump back with small steps <PR number={4339} /> by <GHUser name="0xDRRB" />
- rename esp32 CAN to TWAI, so it compiles again <PR number={4334} /> by <GHUser name="designer2k2" />
- Fix BME280 initialization before wifi setup <PR number={4190} /> by <GHUser name="pxe-la" />
- Add a soft reset in setup() for bmp280 <PR number={4329} /> by <GHUser name="melyux" />

## Release 2022.12.8 - January 27

- Upgrades add-on base image to 6.2.0 <PR number={4310} /> by <GHUser name="frenck" />
- Migrate old-style S6 scripts to s6-rc.d <PR number={4311} /> by <GHUser name="frenck" />
- Refactor NGINX configuration of Home Assistant Add-on <PR number={4312} /> by <GHUser name="frenck" />
- Add Home Assistant integration discovery <PR number={4328} /> by <GHUser name="frenck" />

## Full list of changes

### New Features

- PID Climate - deadband and output sampling <PR number={3254} /> by <GHUser name="patrickcollins12" /> (new-feature)

### New Components

- Add sn74hc165 input shift register <PR number={4151} /> by <GHUser name="jesserockz" /> (new-integration)

### Breaking Changes

- Update esp32 arduino and platform versions <PR number={3564} /> by <GHUser name="jesserockz" /> (notable-change) (breaking-change)

### Beta Changes

- Fix ble parsing with zero padded advertisements <PR number={4162} /> by <GHUser name="jesserockz" />
- Increase watchdog timeout when starting OTA <PR number={4172} /> by <GHUser name="jesserockz" />
- Bump esphome-dashboard to 20221213.0 <PR number={4176} /> by <GHUser name="jesserockz" />
- Remove internal pin restriction from cd74hc4067 <PR number={4179} /> by <GHUser name="jesserockz" />
- Speed up bluetooth proxy connections when using esp-idf <PR number={4171} /> by <GHUser name="bdraco" /> (notable-change)
- Remove warnings when falling through switch cases on purpose <PR number={4181} /> by <GHUser name="jesserockz" />
- Revert camera config change for esp-idf <PR number={4182} /> by <GHUser name="jesserockz" />

### Notable Changes

- Update esp32 arduino and platform versions <PR number={3564} /> by <GHUser name="jesserockz" /> (notable-change) (breaking-change)
- Update ESP-IDF and platform version <PR number={3565} /> by <GHUser name="jesserockz" /> (notable-change)
- Speed up bluetooth proxy connections when using esp-idf <PR number={4171} /> by <GHUser name="bdraco" /> (notable-change)

### All changes

- Bump colorama from 0.4.5 to 0.4.6 <PR number={4010} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Bump frenck/action-yamllint from 1.3.0 to 1.3.1 <PR number={4011} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- add two new icons <PR number={3963} /> by <GHUser name="nagyrobi" />
- Move CONF_INTERRUPT_PIN to const.py <PR number={4014} /> by <GHUser name="jesserockz" />
- Bump pyupgrade from 3.2.0 to 3.2.2 <PR number={4017} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Add reset_reason text sensor to debug component <PR number={3814} /> by <GHUser name="kuba2k2" />
- remove unused variable last_save in total_daily_energy <PR number={4039} /> by <GHUser name="bkaufx" />
- Bump base image versions to latest <PR number={4016} /> by <GHUser name="jesserockz" />
- Add number device class support <PR number={4042} /> by <GHUser name="frenck" />
- Update esp32 arduino and platform versions <PR number={3564} /> by <GHUser name="jesserockz" /> (notable-change) (breaking-change)
- Update esp32 boards list <PR number={4056} /> by <GHUser name="jesserockz" />
- Add Lolin S2 Mini board <PR number={3264} /> by <GHUser name="jhamhader" />
- Add pin aliases for upesy boards <PR number={3794} /> by <GHUser name="uPesy" />
- Bump pylint from 2.15.5 to 2.15.6 <PR number={4058} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- fix display_menu recursive schema gen <PR number={4045} /> by <GHUser name="glmnet" />
- Remove manual handling of ethernet power pin <PR number={4062} /> by <GHUser name="jesserockz" />
- Bump aioesphomeapi from 11.4.3 to 12.0.0 <PR number={4070} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Update ESP-IDF and platform version <PR number={3565} /> by <GHUser name="jesserockz" /> (notable-change)
- Always use brackets around single log macros <PR number={4072} /> by <GHUser name="jesserockz" />
- Midea ir templatable code <PR number={4053} /> by <GHUser name="reidprojects" />
- Boards: Adafruit QT Py ESP32 Pico/C3/S2 <PR number={4064} /> by <GHUser name="jenscski" />
- Dont set esp32 pin drive strength unless pin is set for output <PR number={4075} /> by <GHUser name="jesserockz" />
- Don't allow debug component if you don't have at least debug logging level <PR number={4076} /> by <GHUser name="jesserockz" />
- Remove a bunch of unnecessary pylint disabling <PR number={4079} /> by <GHUser name="jesserockz" />
- Update ESP32-audioI2S library <PR number={4073} /> by <GHUser name="jesserockz" />
- Add vector includes <PR number={4080} /> by <GHUser name="jesserockz" />
- Allow manually specifying binary file to OTA <PR number={4054} /> by <GHUser name="jesserockz" />
- Fix regressed 'esphome run' <PR number={4094} /> by <GHUser name="mmakaay" />
- Make parse_characteristics and parse_descriptors lazy to reduce memory pressure <PR number={4063} /> by <GHUser name="bdraco" />
- Speed up BLE connections <PR number={4093} /> by <GHUser name="bdraco" />
- Align BLE service logging <PR number={4095} /> by <GHUser name="bdraco" />
- Suppress logging unhandled ESP_BLE_AD_TYPE_INT_RANGE <PR number={4096} /> by <GHUser name="bdraco" />
- Make sure all BLE address strings are uppercase <PR number={4097} /> by <GHUser name="bdraco" />
- Add logging for BLE connection slots <PR number={4098} /> by <GHUser name="bdraco" />
- Fix registering for gatt notify when characteristic only support indicate <PR number={4092} /> by <GHUser name="bdraco" />
- Fix descriptors not being deleted <PR number={4104} /> by <GHUser name="bdraco" />
- Improve reliability of bluetooth active connections <PR number={4049} /> by <GHUser name="bdraco" />
- Always use generated api options from aioesphomeapi <PR number={4116} /> by <GHUser name="jesserockz" />
- Fix bluetooth_gatt_write_descriptor never getting a response <PR number={4112} /> by <GHUser name="bdraco" />
- Combine the scanner failure check so we only stop the scanner once <PR number={4111} /> by <GHUser name="bdraco" />
- Reduce memory needed to send the services list <PR number={4110} /> by <GHUser name="bdraco" />
- Avoid parsing characteristics and descriptors to reduce memory pressure <PR number={4109} /> by <GHUser name="bdraco" />
- Send device address type alongside ble advertisements <PR number={4115} /> by <GHUser name="jesserockz" />
- Remove uuid lookups from BLE read/write/notify characteristics <PR number={4102} /> by <GHUser name="bdraco" />
- Active BLE connection v3 <PR number={4113} /> by <GHUser name="bdraco" />
- preprocess out proto_enum_to_string if not needed <PR number={4119} /> by <GHUser name="jesserockz" />
- Bump aioesphomeapi from 12.0.0 to 12.2.0 <PR number={4120} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Avoid parsing services with v3 connections without cache <PR number={4117} /> by <GHUser name="bdraco" />
- FIX: Unnecessary flash writes by ModbusSwitch component <PR number={3648} /> by <GHUser name="jpeletier" />
- Speed up uuid parsing when fetching the service list <PR number={4121} /> by <GHUser name="bdraco" />
- modbus_switch: honor switch:restore_mode <PR number={4122} /> by <GHUser name="jpeletier" />
- PID Climate - deadband and output sampling <PR number={3254} /> by <GHUser name="patrickcollins12" /> (new-feature)
- Allow auto setting of ble client id if there is only 1 in config <PR number={4125} /> by <GHUser name="jesserockz" />
- fix RESTORE_INVERTED switch:restore_mode <PR number={4129} /> by <GHUser name="jpeletier" />
- Bump pylint from 2.15.6 to 2.15.7 <PR number={4127} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Allow global setting of compile process limit <PR number={4081} /> by <GHUser name="jesserockz" />
- Bump flake8 from 5.0.4 to 6.0.0 <PR number={4084} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Bump arduino pico and update wifi methods <PR number={4144} /> by <GHUser name="jesserockz" />
- Bump aioesphomeapi from 12.2.0 to 12.2.1 <PR number={4133} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Bump esptool from 3.3.1 to 4.4 <PR number={4071} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Fix mismatched flake8 version in pre-commit <PR number={4145} /> by <GHUser name="jesserockz" />
- Detect BLE stack unrecoverable state <PR number={4128} /> by <GHUser name="bdraco" />
- Bump pylint from 2.15.7 to 2.15.8 <PR number={4148} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Fix crash with bluetooth tracker and esp-idf <PR number={4140} /> by <GHUser name="bdraco" />
- Use ENTITY_CATEGORY_CONFIG for sprinkler config switches. <PR number={4141} /> by <GHUser name="schinckel" />
- fix switch initialization <PR number={4153} /> by <GHUser name="ssieb" />
- Change 4 chip limit on sn74hc595 to 256 <PR number={4108} /> by <GHUser name="jesserockz" />
- Add reports fahrenheit to tuya climate <PR number={4032} /> by <GHUser name="jesserockz" />
- Allow dashboard import to pull complete file from github <PR number={3982} /> by <GHUser name="jesserockz" />
- Invalid config for waveshare models with no full_update_every <PR number={4066} /> by <GHUser name="Nizzle" />
- Add sn74hc165 input shift register <PR number={4151} /> by <GHUser name="jesserockz" /> (new-integration)
- Fixed deep sleep for ESP32C3 <PR number={4143} /> by <GHUser name="pistifonok" />
- ESP32 IDF: Override toolchain-esp32ulp with espressifs own published version <PR number={4155} /> by <GHUser name="jesserockz" />
- Add API for dashboard to get boards list <PR number={4154} /> by <GHUser name="jesserockz" />
- Bump pytest-asyncio from 0.20.1 to 0.20.2 <PR number={4019} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Bump pyupgrade from 3.2.2 to 3.3.0 <PR number={4137} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Bump dessant/lock-threads from 3 to 4 <PR number={4150} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Bump aioesphomeapi from 12.2.1 to 13.0.1 <PR number={4149} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Bump esphome-dashboard to 20221207.0 <PR number={4156} /> by <GHUser name="jesserockz" />
- Fix ble parsing with zero padded advertisements <PR number={4162} /> by <GHUser name="jesserockz" />
- Increase watchdog timeout when starting OTA <PR number={4172} /> by <GHUser name="jesserockz" />
- Bump esphome-dashboard to 20221213.0 <PR number={4176} /> by <GHUser name="jesserockz" />
- Remove internal pin restriction from cd74hc4067 <PR number={4179} /> by <GHUser name="jesserockz" />
- Speed up bluetooth proxy connections when using esp-idf <PR number={4171} /> by <GHUser name="bdraco" /> (notable-change)
- Remove warnings when falling through switch cases on purpose <PR number={4181} /> by <GHUser name="jesserockz" />
- Revert camera config change for esp-idf <PR number={4182} /> by <GHUser name="jesserockz" />

{/* markdownlint-enable MD013 */}

## All Changelogs

