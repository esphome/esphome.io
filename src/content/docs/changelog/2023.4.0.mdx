---
description: "Changelog for ESPHome 2023.4.0."
title: "ESPHome 2023.4.0 - 19th April 2023"
pagefind: false
slug: "changelog/2023.4.0"
---

import PR from "@components/PR.astro";
import GHUser from "@components/GHUser.astro";


- [Voice Assistant](/components/voice_assistant/)
- [Microphone Core](/components/microphone/index/)
- [I2S Microphone](/components/microphone/i2s_audio/)
- [MMC5603](/components/sensor/mmc5603/)
- [KSZ8081 Ethernet](/components/ethernet/)


## Voice Assistant

This year is the Year of the Voice for Home Assistant, and ESPHome is charging ahead with this in mind.
We've added a new [Voice Assistant](/components/voice_assistant/) component that allows you to use ESPHome devices
as an input for [assist](https://www.home-assistant.io/voice_control/) in Home Assistant **2023.5 or later**.

With this also comes preliminary [microphone](/components/microphone/index/) support, which has been
built in a way that multiple components, like `voice_assistant` can request start / stop of the microphone and get
the data. We hope this leads to more interesting use cases for the microphone in the future.

Home Assistant is hosting a live stream all about the Year of the Voice - Chapter 2.

{/* markdownlint-disable-next-line MD033 */}
<iframe width="560" height="315" src="https://www.youtube-nocookie.com/embed/Tk-pnm7FY7c"
        title="YouTube video player" frameborder="0"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
        allowfullscreen>
</iframe>

## Keith joins Nabu Casa

Nabu Casa is pleased to announce that long time contributor Keith Burzinski (<GHUser name="kbx81" />) is joining the
team as a full time developer to help out on ESPHome. Keith created the [Sprinkler](/components/sprinkler/),
[Thermostat](/components/climate/thermostat/), a bunch of the `ssd` display components and a few other components
as well as fixing many bug along the way. I expect his house climate and garden area are kept well in line.

Looking forward to working with you Keith.

{/* markdownlint-disable MD013 */}

## Release 2023.4.1 - April 24

- fix flip_x <PR number={4727} /> by <GHUser name="ssieb" />
- Use proper schema for delta filter <PR number={4723} /> by <GHUser name="jesserockz" />

## Release 2023.4.2 - April 27

- I2c scan recovery reset fix <PR number={4724} /> by <GHUser name="gcopeland" />
- Debug component doesn't work on RP2040 <PR number={4728} /> by <GHUser name="HeMan" />
- Only request VA port from first client that is subscribed <PR number={4747} /> by <GHUser name="jesserockz" />
- Don't allow fingerprint_grow enroll cancellation when no enrollment started <PR number={4745} /> by <GHUser name="itpeters" />

## Release 2023.4.3 - May 2

- Fix sprinkler switch restore_mode <PR number={4756} /> by <GHUser name="kbx81" />
- Fix i2s media player on devices with no internal DAC <PR number={4768} /> by <GHUser name="jesserockz" />

## Release 2023.4.4 - May 4

- Fixes for Arduino 2.7.4 (for FastLED) <PR number={4777} /> by <GHUser name="timn" />

## Breaking Changes

### I²S Media Player

With the introduction of the [I2S Audio](/components/microphone/i2s_audio/), the media player platform has some
required breaking changes to the YAML configuration. This involves moving the `i2s_lrclk_pin` and
`i2s_bclk_pin` to a new [I2S Audio](/components/i2s_audio/) component.

```yaml
# Before
media_player:
  - platform: i2s_audio
    name: ESPHome I2S Media Player
    dac_type: external
    i2s_lrclk_pin: GPIO33
    i2s_bclk_pin: GPIO19
    i2s_dout_pin: GPIO22
    mode: mono

# After
i2s_audio:
  i2s_lrclk_pin: GPIO33
  i2s_bclk_pin: GPIO19

media_player:
  - platform: i2s_audio
    name: ESPHome I2S Media Player
    dac_type: external
    i2s_dout_pin: GPIO22
    mode: mono
```

### Default restore mode for Switches, Fans and Lights

The default `restore_mode` for switches, fans and lights has been changed
from `RESTORE_DEFAULT_OFF` to `ALWAYS_OFF` with the intention that restoring values
from flash should be manually specified by a user if desired in their YAML configuration.

### Number `step`

The `number` components have always internally set a `step` required in config,
but allowed it to be not specified at all. This caused issues on the Home Assistant side
of things. This is only a breaking change for external components that have a `number` platform.

### UART ids

Due to `uart0` / `uart1` / `uart2` being defined in some of the platform code ESPHome uses,
ESPHome will now disallow these ids from being used in the config. You can simply change them to
`uart_0` to continue using.

## Full list of changes

### New Components

- Added in mmc5603 code <PR number={4175} /> by <GHUser name="benhoff" /> (new-integration)
- Add push to talk voice assistant <PR number={4648} /> by <GHUser name="jesserockz" /> (new-integration)

### Breaking Changes

- Disallow uart0/1/2 as ids in config <PR number={4446} /> by <GHUser name="jesserockz" /> (breaking-change)
- Require step to be set when calling register_number <PR number={4622} /> by <GHUser name="jesserockz" /> (breaking-change)
- Fix restore <PR number={4655} /> by <GHUser name="spacemanspiff2007" /> (breaking-change)

### Beta Changes

- debug component, allow without debug logging <PR number={4685} /> by <GHUser name="jesserockz" />
- Fixed dns2 for ethernet <PR number={4698} /> by <GHUser name="HeMan" />
- Add timeout to i2c write error logs <PR number={4697} /> by <GHUser name="Szewcson" />
- Add event triggers to voice_assistant <PR number={4699} /> by <GHUser name="jesserockz" />
- Call on_error if no api client connected that handles voice <PR number={4709} /> by <GHUser name="jesserockz" />
- Add ethernet powerdown (fixes esphome/issues#4420) <PR number={4706} /> by <GHUser name="tracestep" />
- Bump arduino platform version to 5.3.0 <PR number={4713} /> by <GHUser name="jesserockz" />

### All changes

- Mark unique_id() virtual method as deprecated <PR number={4538} /> by <GHUser name="oxan" />
- Drop deprecated entity property base methods <PR number={4539} /> by <GHUser name="oxan" />
- Format test files <PR number={4541} /> by <GHUser name="jesserockz" />
- EntityBase: Icon string can stay in flash. <PR number={4566} /> by <GHUser name="Fabian-Schmidt" />
- Update the delta filter to be able to take a percentage change <PR number={4391} /> by <GHUser name="TrentHouliston" />
- Added missing PM_1_0 and PM_10_0 for PMS5003T and PMS5003ST <PR number={4560} /> by <GHUser name="genestealer" />
- Add an option to force SPI into software mode <PR number={4556} /> by <GHUser name="wupeka" />
- allow using a binary output for the status led <PR number={4532} /> by <GHUser name="ssieb" />
- Add option flip_x <PR number={4555} /> by <GHUser name="rafal83" />
- Bump aioesphomeapi from 13.5.0 to 13.5.1 <PR number={4572} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Verbose output of define. <PR number={4576} /> by <GHUser name="Fabian-Schmidt" />
- Add support for ESP32 CAM 3MP and 5MP resolutions and arbitrary camera clock. <PR number={4580} /> by <GHUser name="peterhalicky" />
- Add a simple 'skip_initial' filter <PR number={4582} /> by <GHUser name="sybrenstuvel" />
- Fix outdated filter string in platformio_api <PR number={4587} /> by <GHUser name="LordMike" />
- Split test3.yaml <PR number={4591} /> by <GHUser name="jesserockz" />
- Disallow uart0/1/2 as ids in config <PR number={4446} /> by <GHUser name="jesserockz" /> (breaking-change)
- Upgrade clang-format to v13 <PR number={4535} /> by <GHUser name="oxan" />
- B/W support for GooDisplay GDEY029T94 (as used on Adafruit MagTag) <PR number={4222} /> by <GHUser name="AaronJackson" />
- Fix negative sqrt root in ct_clamp_sensor.cpp <PR number={4236} /> by <GHUser name="jerome992" />
- Wrap ipv6 code a bit more <PR number={4574} /> by <GHUser name="jesserockz" />
- Remove EntityBase from sprinkler <PR number={4606} /> by <GHUser name="jesserockz" />
- Fix wrong port multiplexer name in dump GPIO function of sx1509 <PR number={4592} /> by <GHUser name="skaldo" />
- Bump zeroconf from 0.47.3 to 0.47.4 <PR number={4597} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Bump pytest-asyncio from 0.20.3 to 0.21.0 <PR number={4599} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Limit range on filter time period for remote_receiver <PR number={4604} /> by <GHUser name="RoboMagus" />
- add select_schema to select component <PR number={4545} /> by <GHUser name="regevbr" />
- Add ability to clear the gatt cache <PR number={4621} /> by <GHUser name="bdraco" />
- Bump actions/stale from 7 to 8 <PR number={4615} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Require step to be set when calling register_number <PR number={4622} /> by <GHUser name="jesserockz" /> (breaking-change)
- Lower range of CONF_FREQUENCY <PR number={4619} /> by <GHUser name="berendhaan" />
- Allow entity names to be set to None <PR number={4607} /> by <GHUser name="jesserockz" />
- Fix platform restriction for bme680_bsec <PR number={4616} /> by <GHUser name="jesserockz" />
- Fix EzoCommandType enum <PR number={4593} /> by <GHUser name="alfredopironti" />
- `EntityBase` Name can stay in flash. <PR number={4594} /> by <GHUser name="Fabian-Schmidt" />
- feat: Add support to unsubscribe from BLE advertisements <PR number={4620} /> by <GHUser name="richardhopton" />
- EntityBase: Move ObjectId to Flash <PR number={4569} /> by <GHUser name="Fabian-Schmidt" />
- Add workflow to sync device classes with HA dev <PR number={4629} /> by <GHUser name="jesserockz" />
- Synchronise Device Classes from Home Assistant <PR number={4633} /> by [@github-actions[bot]](https://github.com/github-actions[bot])
- Remove AUTO_LOAD from as3935 <PR number={4630} /> by <GHUser name="jesserockz" />
- Add ifdef to new bt proxy unsubscribe <PR number={4634} /> by <GHUser name="jesserockz" />
- add bluetooth mac address in dump_config() <PR number={4628} /> by <GHUser name="felixlungu" />
- Binary map bugfixes <PR number={4636} /> by <GHUser name="kahrendt" />
- entity_base avoid padding bytes. <PR number={4637} /> by <GHUser name="Fabian-Schmidt" />
- Avoid sensor padding. <PR number={4638} /> by <GHUser name="Fabian-Schmidt" />
- Support advanced UART customization <PR number={4465} /> by <GHUser name="Fabian-Schmidt" />
- Log calibration results at level INFO <PR number={4240} /> by <GHUser name="Duckle29" />
- Number step not optional <PR number={4649} /> by <GHUser name="RoboMagus" />
- VSCode / devcontainer updates <PR number={4647} /> by <GHUser name="jesserockz" />
- Retry PN532_COMMAND_VERSION_DATA on setup() (fixes esphome/issues#3823) <PR number={4651} /> by <GHUser name="tracestep" />
- Bump pylint from 2.16.4 to 2.17.2 <PR number={4650} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Bump black from 23.1.0 to 23.3.0 <PR number={4635} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Added in mmc5603 code <PR number={4175} /> by <GHUser name="benhoff" /> (new-integration)
- fix compilation with latest esp-idf <PR number={4671} /> by <GHUser name="Mic92" />
- Bump zeroconf from 0.47.4 to 0.56.0 <PR number={4674} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Bump pytest from 7.2.2 to 7.3.0 <PR number={4673} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Add push to talk voice assistant <PR number={4648} /> by <GHUser name="jesserockz" /> (new-integration)
- Fix ESP32 SPI hardware assignment in Arduino fw <PR number={4669} /> by <GHUser name="kbx81" />
- Fix some NFC/PN532 crashes <PR number={4678} /> by <GHUser name="kbx81" />
- Add support for SSD1306 72x40 displays <PR number={4659} /> by <GHUser name="johnsto" />
- Fix pin schema for i2s microphone <PR number={4680} /> by <GHUser name="jesserockz" />
- Fix BedJet setup priority [fixes esphome/issues#3807] <PR number={4677} /> by <GHUser name="jhansche" />
- Add always trigger stop <PR number={4249} /> by <GHUser name="X-Ryl669" />
- Fix graph limits for negative values and other corner cases <PR number={4253} /> by <GHUser name="unhold" />
- Fix cut-off on 2.13" waveshare/ttgo epaper displays <PR number={4255} /> by <GHUser name="unhold" />
- [Ethernet] Add PHY KSZ8081 support <PR number={4668} /> by <GHUser name="Fabian-Schmidt" />
- Fix restore <PR number={4655} /> by <GHUser name="spacemanspiff2007" /> (breaking-change)
- debug component, allow without debug logging <PR number={4685} /> by <GHUser name="jesserockz" />
- Fixed dns2 for ethernet <PR number={4698} /> by <GHUser name="HeMan" />
- Add timeout to i2c write error logs <PR number={4697} /> by <GHUser name="Szewcson" />
- Add event triggers to voice_assistant <PR number={4699} /> by <GHUser name="jesserockz" />
- Call on_error if no api client connected that handles voice <PR number={4709} /> by <GHUser name="jesserockz" />
- Add ethernet powerdown (fixes esphome/issues#4420) <PR number={4706} /> by <GHUser name="tracestep" />
- Bump arduino platform version to 5.3.0 <PR number={4713} /> by <GHUser name="jesserockz" />

{/* markdownlint-enable MD013 */}

## All Changelogs


