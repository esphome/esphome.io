---
description: "Changelog for ESPHome 2023.10.0."
title: "ESPHome 2023.10.0 - 18th October 2023"
pagefind: false
slug: "changelog/2023.10.0"
---

import PR from "@components/PR.astro";
import GHUser from "@components/GHUser.astro";


- [MMC5983](/components/sensor/mmc5983/)

This release is quite small as myself (<GHUser name="jesserockz" />) and <GHUser name="kbx81" /> have been
focusing on `voice_assistant` updates and preparations for the Year of the Voice - Chapter 4.

Check out the stream replay here:

{/* markdownlint-disable-next-line MD033 */}
<iframe width="560" height="315" src="https://www.youtube-nocookie.com/embed/YzgYYkOrnhQ"
        title="YouTube video player" frameborder="0"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
        allowfullscreen>
</iframe>

## XOR Condition

A new `xor` condition has been added to the automation engine. This condition will only be true
when a single condition in the list is true.

{/* markdownlint-disable MD013 */}

## Release 2023.10.1 - October 19

- Fix voice_assistant without a speaker <PR number={5558} /> by <GHUser name="jesserockz" />
- esp32_improv add timeout <PR number={5556} /> by <GHUser name="jesserockz" />
- Create IPv4 sockets if ipv6 is not enabled <PR number={5565} /> by <GHUser name="jesserockz" />
- Incorrect ESP32 Strapping PIN Defined <PR number={5563} /> by <GHUser name="descipher" />

## Release 2023.10.2 - October 24

- Fix XOR condition <PR number={5567} /> by <GHUser name="jesserockz" />
- Improv Serial support via USB CDC and JTAG <PR number={5559} /> by <GHUser name="kbx81" />
- Publish the `pulse_meter` total when setting the total <PR number={5475} /> by <GHUser name="TrentHouliston" />
- Remove explicit cast for IPAddress <PR number={5574} /> by <GHUser name="HeMan" />
- Set addr type when copy from ip4_addr_t <PR number={5583} /> by <GHUser name="HeMan" />
- fix canbus send config <PR number={5585} /> by <GHUser name="ssieb" />
- Allow set climate preset to NONE <PR number={5588} /> by <GHUser name="dentra" />

## Release 2023.10.3 - October 24

- Set IP address `type` only when IPv4 and IPv6 are both enabled <PR number={5595} /> by <GHUser name="kbx81" />

## Release 2023.10.4 - October 30

- Update current_based_cover bugfix <PR number={5587} /> by <GHUser name="rogerbusser" />
- Fixes ip include on arduino 2.7.4 <PR number={5620} /> by <GHUser name="HeMan" />
- Fix bug when requesting italic gfonts <PR number={5623} /> by <GHUser name="dewet22" />
- Handle enum type in tuya text_sensor <PR number={5626} /> by <GHUser name="jesserockz" />

## Release 2023.10.5 - November 1

- Add connection triggers to api <PR number={5628} /> by <GHUser name="jesserockz" />
- Add on_client_connected and disconnected to voice assistant <PR number={5629} /> by <GHUser name="jesserockz" />
- Ensure that all uses of strncpy in wifi component are safe. <PR number={5636} /> by <GHUser name="kpfleming" />
- Remove some explicit IPAddress casts <PR number={5639} /> by <GHUser name="HeMan" />

## Release 2023.10.6 - November 3

- Revert "Ensure that all uses of strncpy in wifi component are safe." <PR number={5662} /> by <GHUser name="jesserockz" />

## Breaking changes

The SHT3x had a `heater_enabled` config option added in the 2023.9.0 release with a default value of `true`.
This has been flipped this release to be `false` as it started causing issues with various sensors.

The SPS30 number concentration sensors had their units fixed to be `#/cm³` instead of `#/m³`.

## Full list of changes

### New Components

- Implement sensor component for MMC5983 <PR number={5361} /> by <GHUser name="agoode" /> (new-integration)

### Breaking Changes

- Do not enable SHT3x heater by default. Fixes #4886. <PR number={5445} /> by <GHUser name="jkl1337" /> (breaking-change)
- Fix units for SPS30 number concentration sensors <PR number={5452} /> by <GHUser name="kahrendt" /> (breaking-change)

### Beta Changes

- Update htu21d.cpp, fix publishing of heater level <PR number={5520} /> by <GHUser name="Nippey" />
- BD5758D - Add delays and ACKs <PR number={5524} /> by <GHUser name="Cossid" />
- Update Improv BLE component <PR number={5518} /> by <GHUser name="jesserockz" />
- SM10BIT_BASE - Add delays and ACKs, clear all channels before sleeping. <PR number={5526} /> by <GHUser name="Cossid" />
- BP1658CJ - Clear all channels before sleeping. <PR number={5525} /> by <GHUser name="Cossid" />
- Prometheus fix for esp-idf and fix newlines <PR number={5536} /> by <GHUser name="jesserockz" />
- Make IPAddress's operator!= compare values, not memory addresses. <PR number={5537} /> by <GHUser name="raineth" />
- Add change i2c address and allow multi conf for TB6612FNG <PR number={5492} /> by <GHUser name="max246" />
- Add stream start and end events <PR number={5545} /> by <GHUser name="jesserockz" />
- Fix esp32_improv authorizer with no binary sensors in config <PR number={5546} /> by <GHUser name="jesserockz" />
- More voice assistant fixes <PR number={5547} /> by <GHUser name="jesserockz" />
- Fix default libretiny manufacturer reported to HA <PR number={5549} /> by <GHUser name="jesserockz" />
- SML: fix incomplete sign extension for abbreviated transmissions <PR number={5544} /> by <GHUser name="fblaese" />
- esp32_improv advertise capabilities and state in ble service data <PR number={5553} /> by <GHUser name="jesserockz" />

### Notable Changes

- Add xor automation condition <PR number={5453} /> by <GHUser name="kahrendt" /> (notable-change)

### All changes

- SX1509 component <PR number={5385} /> by <GHUser name="vr6racer" />
- Ci find YAML tests dynamically <PR number={5399} /> by <GHUser name="Fabian-Schmidt" />
- Make ESPHome data dir configurable <PR number={5417} /> by <GHUser name="werdnum" />
- Bump actions/checkout from 4.0.0 to 4.1.0 <PR number={5420} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Fix typo in documentation <PR number={5425} /> by <GHUser name="kapily" />
- Bump pylint from 2.17.5 to 2.17.6 <PR number={5429} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Refactor ip address representation <PR number={5252} /> by <GHUser name="HeMan" />
- Fix .esphome path when not using envvar <PR number={5440} /> by <GHUser name="jesserockz" />
- [BP1658CJ] Missing clock line delays and ack bit <PR number={5448} /> by <GHUser name="leoshusar" />
- [ssd1351] fix: wait for the component to be at least in setup phase b… <PR number={5454} /> by <GHUser name="xdecock" />
- Do not enable SHT3x heater by default. Fixes #4886. <PR number={5445} /> by <GHUser name="jkl1337" /> (breaking-change)
- Add testcases for multiple SPI buses on ESP32 Arduino <PR number={5457} /> by <GHUser name="angelnu" />
- Feat/component poller suspend <PR number={5423} /> by <GHUser name="xdecock" />
- Bump actions/setup-python from 4.7.0 to 4.7.1 <PR number={5467} /> by [@dependabot[bot]](https://github.com/dependabot[bot])
- Add xor automation condition <PR number={5453} /> by <GHUser name="kahrendt" /> (notable-change)
- Fix units for SPS30 number concentration sensors <PR number={5452} /> by <GHUser name="kahrendt" /> (breaking-change)
- ST7789v - Allow predefined backlight pin to be disabled. <PR number={5476} /> by <GHUser name="clydebarrow" />
- MAX7219 - Update intensity <PR number={5477} /> by <GHUser name="clydebarrow" />
- St7789v and SPI data rate <PR number={5472} /> by <GHUser name="clydebarrow" />
- Move CONF_IRQ_PIN into const.py <PR number={5488} /> by <GHUser name="kbx81" />
- Initial ESP32-H2 Support <PR number={5498} /> by <GHUser name="vidplace7" />
- Increased debug message precision <PR number={5496} /> by <GHUser name="lukeansell" />
- Support for Haier IR protocol added <PR number={5403} /> by <GHUser name="paveldn" />
- add USE_SPI define <PR number={5500} /> by <GHUser name="angelnu" />
- Implement sensor component for MMC5983 <PR number={5361} /> by <GHUser name="agoode" /> (new-integration)
- Bump docker dependencies <PR number={5501} /> by <GHUser name="jesserockz" />
- Move to Pillow 10.x <PR number={5489} /> by <GHUser name="hostcc" />
- [Sprinkler] Initialize timers early to avoid crash <PR number={5499} /> by <GHUser name="hostcc" />
- Deep sleep is only available on esp32 and esp8266 <PR number={5507} /> by <GHUser name="jesserockz" />
- Use platform consts <PR number={5508} /> by <GHUser name="jesserockz" />
- Fixed precision for Nextion sensor with float values <PR number={5497} /> by <GHUser name="olegtarasov" />
- Remote wake word support for voice assistant <PR number={5229} /> by <GHUser name="jesserockz" />
- Don't allow entity category "CONFIG" for sensors <PR number={5505} /> by <GHUser name="edenhaus" />
- Prepare protobuf for ESP-IDF ≥ 5 <PR number={5510} /> by <GHUser name="HeMan" />
- fix build lang schema for spi and i2c <PR number={5509} /> by <GHUser name="glmnet" />
- Added Nextion display error handling during setup <PR number={5493} /> by <GHUser name="olegtarasov" />
- Fix e131 and voice_assistant sockets <PR number={5502} /> by <GHUser name="justdaniel-gh" />
- Allow manual set "Invert_display" <PR number={5494} /> by <GHUser name="nielsnl68" />
- As3935 calibration <PR number={5366} /> by <GHUser name="mveinot" />
- Small fixes for voice assistant <PR number={5513} /> by <GHUser name="synesthesiam" />
- Add detail param to allow listing of select options in WebServer REST API <PR number={5503} /> by <GHUser name="DanielBaulig" />
- Bump curl to 7.74.0-1.3+deb11u10 <PR number={5517} /> by <GHUser name="jesserockz" />
- Update htu21d.cpp, fix publishing of heater level <PR number={5520} /> by <GHUser name="Nippey" />
- BD5758D - Add delays and ACKs <PR number={5524} /> by <GHUser name="Cossid" />
- Update Improv BLE component <PR number={5518} /> by <GHUser name="jesserockz" />
- SM10BIT_BASE - Add delays and ACKs, clear all channels before sleeping. <PR number={5526} /> by <GHUser name="Cossid" />
- BP1658CJ - Clear all channels before sleeping. <PR number={5525} /> by <GHUser name="Cossid" />
- Prometheus fix for esp-idf and fix newlines <PR number={5536} /> by <GHUser name="jesserockz" />
- Make IPAddress's operator!= compare values, not memory addresses. <PR number={5537} /> by <GHUser name="raineth" />
- Add change i2c address and allow multi conf for TB6612FNG <PR number={5492} /> by <GHUser name="max246" />
- Add stream start and end events <PR number={5545} /> by <GHUser name="jesserockz" />
- Fix esp32_improv authorizer with no binary sensors in config <PR number={5546} /> by <GHUser name="jesserockz" />
- More voice assistant fixes <PR number={5547} /> by <GHUser name="jesserockz" />
- Fix default libretiny manufacturer reported to HA <PR number={5549} /> by <GHUser name="jesserockz" />
- SML: fix incomplete sign extension for abbreviated transmissions <PR number={5544} /> by <GHUser name="fblaese" />
- esp32_improv advertise capabilities and state in ble service data <PR number={5553} /> by <GHUser name="jesserockz" />

{/* markdownlint-enable MD013 */}

## All Changelogs
