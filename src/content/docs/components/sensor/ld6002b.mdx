---
description: "Instructions for setting up LD6002B 3D presence radar sensors."
title: "LD6002B 3D Presence Radar"
---

import Figure from '@components/Figure.astro';
import APIRef from '@components/APIRef.astro';
import ld6002bImg from './images/ld6002b.jpg';

## Component

<span id="ld6002b-component"></span>

The `ld6002b` component allows you to use the Hi-Link HLK-LD6002B 60 GHz 3D presence radar module with
ESPHome. It can track up to 3 people and reports per-target position (X/Y/Z), cluster ID, and Doppler
index.

The [UART](/components/uart) component is required. Use 115200 baud, 8 data bits, no parity, and 1
stop bit.

<Figure src={ld6002bImg} alt="HLK-LD6002B module" caption="HLK-LD6002B 3D presence radar module" />

```yaml
# Example configuration entry
ld6002b:
```

### Configuration Variables

- **id** (*Optional*, [ID](/guides/configuration-types#id)): Manually specify the ID for this component.
- **uart_id** (*Optional*, [ID](/guides/configuration-types#id)): Manually specify the ID of the
  [UART Component](/components/uart) to use. Required if you have multiple UARTs configured.
- **wakeup_pin** (*Optional*, [Pin Schema](/guides/configuration-types#pin-schema)): Pin used to wake the
  radar from low power mode. Required if you want `auto_wake` or the `wake` button to function.
- **wakeup_pulse_ms** (*Optional*, [Time](/guides/configuration-types#time), default: `50ms`): Pulse
  length for the wakeup pin.
- **auto_wake** (*Optional*, boolean, default: `true`): When enabled *and* a `wakeup_pin` is
  configured, the component pulses `wakeup_pin` before sending a command. Has no effect if no
  `wakeup_pin` is set.
- **max_data_len** (*Optional*, int, range `256`–`65535`): Maximum frame payload length in bytes. When
  unset, defaults to `4096` if point-cloud output is configured (the `point_count` sensor or the
  `point_cloud` switch) and `1024` otherwise. Increase only if you enable point-cloud output and see
  dropped frames.
- **throttle** (*Optional*, [Time](/guides/configuration-types#time), default: `1s`): Minimum time
  between sensor updates. The radar sends data very frequently; use this to reduce the update rate.

> [!NOTE]
> Point-cloud output can generate much larger frames. On memory-constrained devices, keep `max_data_len` low unless you enable point-cloud output and need the larger buffer.

## Power and Hardware Requirements

> [!WARNING]
> The module is sensitive to power quality. Supply 3.1–3.5 V with ripple ≤ 50 mV and current capacity
> ≥ 1 A. For DC-DC supplies, use a switching frequency of at least 2 MHz.

- GPIO IO voltage is 3.3 V.
- The TX2 pin can output a simple presence signal (high when presence is detected).
- Boot requires BOOT1 held low before startup (BOOT pins are pulled up internally).

## Specifications

- Operating frequency: 58–64 GHz.
- Emission power: 12 dBm; antenna gain: 4 dBi.
- Beam width: ±60° horizontal and vertical (-3 dB).
- Operating temperature: -20 to 85 °C.
- Maximum tracked targets: 3.
- Distance accuracy: ~0.4 m; identification accuracy: 95% (module spec).

## Installation and Coordinate System

The module supports top and side mounting. The horizontal and vertical beam angles are ±60° and the
maximum detection distance is 6 m. A typical ceiling installation at 2.7 m height yields a detection
area of roughly 5 m by 3 m.

Target and area coordinates are reported in meters. The origin is the module projection onto the
reference plane (floor for ceiling installs, wall for side installs). X is left/right, Y is
forward/away, and Z is height. Detection and interference areas are configured relative to this
origin.

> [!NOTE]
> Detection distance varies with target size and environment. Some variation is expected.

## Area Configuration Notes

Detection areas define where presence is considered valid for the TX2 output and the `area_N` binary
sensors. Interference areas mask known noise sources (fans, air conditioners, curtains) so they do
not trigger presence. Areas are stored in the module and persist across power cycles.

Use `area_id` to select the area, set `area_config`, then press **Apply Area**. Use **Get Areas** to
read back stored values.

> [!NOTE]
> If you change only some coordinates, ESPHome will fetch the current area values and apply the update
> once all coordinates are known. This can take a moment after pressing **Apply Area**.

## Binary Sensor

<span id="ld6002b-binary-sensors"></span>

The `ld6002b` binary sensor provides presence detection for targets and detection areas.

```yaml
binary_sensor:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    target:
      name: Presence
    target_1:
      name: Target-1 Presence
    area_0:
      name: Area-0 Presence
```

### Configuration Variables

- **ld6002b_id** (*Optional*, [ID](/guides/configuration-types#id)): Manually specify the ID of the
  LD6002B component.
- **target** (*Optional*): True if any target is detected. All options from
  [Binary Sensor](/components/binary_sensor#config-binary_sensor).
- **target_N** (*Optional*): True if the specific target (N = 1 to 3) is detected: `target_1`,
  `target_2`, `target_3`. All options from
  [Binary Sensor](/components/binary_sensor#config-binary_sensor).
- **area_N** (*Optional*): True if presence is detected in detection area N (0 to 3): `area_0`,
  `area_1`, `area_2`, `area_3`. All options from
  [Binary Sensor](/components/binary_sensor#config-binary_sensor).

## Sensor

<span id="ld6002b-sensors"></span>

The `ld6002b` sensor provides target coordinates, point-cloud count, and configured area coordinates.

```yaml
sensor:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    target_count:
      name: Target Count
    point_count:
      name: Point Count
    target_1:
      x:
        name: Target-1 X
      y:
        name: Target-1 Y
      z:
        name: Target-1 Z
      dop_idx:
        name: Target-1 Doppler Index
      cluster_id:
        name: Target-1 Cluster
```

### Configuration Variables

- **ld6002b_id** (*Optional*, [ID](/guides/configuration-types#id)): Manually specify the ID of the
  LD6002B component.
- **target_count** (*Optional*, int): Total number of targets currently detected (0 to 3). All options
  from [Sensor](/components/sensor).
- **point_count** (*Optional*, int): Total number of point-cloud points reported by the radar. All
  options from [Sensor](/components/sensor).
- **target_N** (*Optional*): Details about each target (N = 1 to 3). Up to 3 targets can be tracked
  simultaneously: `target_1`, `target_2`, `target_3`.

  - **x** (*Optional*, float): X coordinate of the target in meters.
  - **y** (*Optional*, float): Y coordinate of the target in meters.
  - **z** (*Optional*, float): Z coordinate of the target in meters.
  - **dop_idx** (*Optional*, int): Doppler index for the target (speed indicator, not a direct speed).
  - **cluster_id** (*Optional*, int): Cluster ID for the target.

  All options above inherit from [Sensor](/components/sensor).

- **interference_area_N** (*Optional*): Interference (mask) area coordinates (N = 0 to 3). Each area
  provides `x_min`, `x_max`, `y_min`, `y_max`, `z_min`, `z_max` sensors. All options from
  [Sensor](/components/sensor).

- **detection_area_N** (*Optional*): Detection area coordinates (N = 0 to 3). Each area provides
  `x_min`, `x_max`, `y_min`, `y_max`, `z_min`, `z_max` sensors. All options from
  [Sensor](/components/sensor).

> [!NOTE]
> Target reporting is controlled by the **Target Display** switch. The component enables target
> reporting automatically if any target sensors are configured. If target reporting is disabled,
> target sensors will remain `unknown`.

> [!NOTE]
> If you configure any area sensors or area configuration numbers, the component requests stored
> area data on boot. You can also use **Get Areas** to refresh the values.

> [!NOTE]
> Point count updates are only produced when point-cloud reporting is enabled.

## Text Sensor

<span id="ld6002b-text-sensors"></span>

The `ld6002b` text sensor exposes radar operating mode and firmware version.

```yaml
text_sensor:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    work_mode:
      name: Work Mode
    ota_version:
      name: OTA Version
```

### Configuration Variables

- **ld6002b_id** (*Optional*, [ID](/guides/configuration-types#id)): Manually specify the ID of the
  LD6002B component.
- **work_mode** (*Optional*): `normal` or `low_power` based on the radar work mode. All options from
  [Text Sensor](/components/text_sensor#config-text_sensor).
- **ota_version** (*Optional*): Firmware version string, if supported by your module firmware. All
  options from [Text Sensor](/components/text_sensor#config-text_sensor).

> [!NOTE]
> `work_mode` reflects the current operating state and can differ from the `low_power` switch setting.
> If the module firmware does not emit work mode reports, ESPHome falls back to the configured low-power
> setting after a **Get Low Power Mode** request. In this fallback, ESPHome treats any detected presence
> as `normal`.

> [!NOTE]
> Firmware updates use the vendor OTA upgrade tool; see the HLK OTA documentation provided with the
> module.

## Number

<span id="ld6002b-number"></span>

The `ld6002b` number platform lets you configure timing and detection areas.

```yaml
number:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    hold_delay:
      name: Hold Delay (s)
    z_min:
      name: Z Min (m)
    z_max:
      name: Z Max (m)
    low_power_sleep_time:
      name: Low Power Sleep (ms)
    area_config:
      x_min:
        name: Area X Min
      x_max:
        name: Area X Max
      y_min:
        name: Area Y Min
      y_max:
        name: Area Y Max
      z_min:
        name: Area Z Min
      z_max:
        name: Area Z Max
```

### Configuration Variables

- **ld6002b_id** (*Optional*, [ID](/guides/configuration-types#id)): Manually specify the ID of the
  LD6002B component.
- **hold_delay** (*Optional*, range `0`–`65535` s, step `1`): Presence hold time, in seconds (default
  30 s in the 2025 protocol spec). All options from
  [Number](/components/number#config-number).
- **z_min** / **z_max** (*Optional*, range `-10`–`10` m, step `0.1`): Global Z-axis detection range, in
  meters. All options from
  [Number](/components/number#config-number).
- **low_power_sleep_time** (*Optional*, range `0`–`600000` ms, step `100`): Low power sleep time in
  milliseconds (default 500 ms in the 2025 protocol spec). When low-power mode is enabled and the area
  is unattended, the module sleeps for this interval between scans; higher values reduce idle power but
  increase detection latency. All options from [Number](/components/number#config-number).
- **area_config** (*Optional*): Coordinate values (`x_min`/`x_max`/`y_min`/`y_max`/`z_min`/`z_max`, each
  range `-10`–`10` m, step `0.1`) for the selected area ID. **Requires** both a `select.area_id` and a
  `button.apply_area` on the same `ld6002b` (enforced at compile time); set the values, then press
  **Apply Area** to send them.

> [!NOTE]
> The module documentation recommends keeping the low power sleep time at or below 500 ms.

## Select

<span id="ld6002b-select"></span>

The `ld6002b` select platform configures sensitivity, trigger speed, installation mode, and the
active area ID.

```yaml
select:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    sensitivity:
      name: Sensitivity
    trigger_speed:
      name: Trigger Speed
    installation_mode:
      name: Installation Mode
    area_id:
      name: Area ID
```

### Configuration Variables

- **ld6002b_id** (*Optional*, [ID](/guides/configuration-types#id)): Manually specify the ID of the
  LD6002B component.
- **sensitivity** (*Optional*): `low`, `medium`, or `high`. `low` only detects motion. All options
  from [Select](/components/select#config-select).
- **trigger_speed** (*Optional*): `fast` (~0.5 s), `medium` (~1 s), or `slow` (~5 s) — the time from
  detecting a person to triggering. All options from [Select](/components/select#config-select).
- **installation_mode** (*Optional*): `top` or `side`. All options from
  [Select](/components/select#config-select).
- **area_id** (*Optional*): Area to edit with `area_config`. Options are `interference_0` through
  `interference_3` and `detection_0` through `detection_3` (mapped internally to IDs 0–7). All options
  from [Select](/components/select#config-select).

> [!NOTE]
> If no area ID is selected, **Apply Area** is ignored to prevent writing to the wrong region.

## Switch

<span id="ld6002b-switch"></span>

The `ld6002b` switch platform enables low power mode, point-cloud output, and target reporting.

```yaml
switch:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    low_power:
      name: Low Power
    point_cloud:
      name: Point Cloud
    target_display:
      name: Target Display
```

### Configuration Variables

- **ld6002b_id** (*Optional*, [ID](/guides/configuration-types#id)): Manually specify the ID of the
  LD6002B component.
- **low_power** (*Optional*): Enable or disable low power mode. In low power mode, the module uses a
  lower duty cycle when the area is empty and returns to full tracking on motion. The module
  documentation reports ~2.5 mA average current in low power idle and ~135 mA in normal tracking.
  All options from [Switch](/components/switch#config-switch).
- **point_cloud** (*Optional*): Enable or disable point-cloud reporting (required for `point_count`
  updates). All options from
  [Switch](/components/switch#config-switch).
- **target_display** (*Optional*): Enable or disable target reporting (`0x0A04`). If disabled, target
  sensors will show `unknown` until re-enabled. All options from
  [Switch](/components/switch#config-switch).

> [!NOTE]
> Point-cloud reporting is disabled on boot to reduce traffic. Enable **Point Cloud** to start
> streaming point-cloud data. The full point cloud is not exposed as a text sensor; use
> `point_count` to monitor how many points are returned.
>
> The `low_power` switch enables or disables low-power mode. The `work_mode` text sensor reflects the
> current operating state, which may stay in `normal` while movement is detected.

## Button

<span id="ld6002b-button"></span>

The `ld6002b` button platform exposes configuration and diagnostic commands.

```yaml
button:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    apply_area:
      name: Apply Area
    auto_interference:
      name: Auto Interference
    get_areas:
      name: Get Areas
    get_installation:
      name: Get Installation
    wake:
      name: Wake
```

### Configuration Variables

- **ld6002b_id** (*Optional*, [ID](/guides/configuration-types#id)): Manually specify the ID of the
  LD6002B component.
- **apply_area** (*Optional*): Send the configured `area_config` to the selected `area_id`. **Requires**
  a `select.area_id` on the same `ld6002b`. All options from [Button](/components/button#config-button).
- **auto_interference** (*Optional*): Generate interference areas from the current environment. Run
  this with no people present and with known interference sources active. To reset the generated
  interference areas, use **Clear Interference**. All options from
  [Button](/components/button#config-button).
- **get_areas** (*Optional*): Request stored interference/detection areas from the radar. All options
  from [Button](/components/button#config-button).
- **clear_interference** (*Optional*): Clear interference areas. All options from
  [Button](/components/button#config-button).
- **reset_detection_area** (*Optional*): Reset detection areas to defaults (4 m by 4 m). All options
  from [Button](/components/button#config-button).
- **get_delay** (*Optional*): Request hold delay. All options from [Button](/components/button#config-button).
- **get_sensitivity** (*Optional*): Request sensitivity state. All options from
  [Button](/components/button#config-button).
- **get_trigger_speed** (*Optional*): Request trigger speed state. All options from
  [Button](/components/button#config-button).
- **get_z_range** (*Optional*): Request Z-axis range. All options from
  [Button](/components/button#config-button).
- **get_installation** (*Optional*): Request installation mode from the radar and update the
  `installation_mode` select with `top` or `side`. All options from
  [Button](/components/button#config-button).
- **get_low_power_mode** (*Optional*): Request low power mode state. All options from
  [Button](/components/button#config-button).
- **get_low_power_sleep_time** (*Optional*): Request low power sleep time. All options from
  [Button](/components/button#config-button).
- **reset_unattended** (*Optional*): Reset the module's unattended (no-person) state used by its
  low-power logic. Helpful if the device gets stuck in an unattended state. All options from
  [Button](/components/button#config-button).
- **wake** (*Optional*): Pulse the `wakeup_pin` to wake the radar. **Requires** `wakeup_pin` on the
  parent `ld6002b`. All options from [Button](/components/button#config-button).

## Examples

### Targets Only (X/Y/Z For 3 Targets)

```yaml
uart:
  tx_pin: GPIOXX
  rx_pin: GPIOYY
  baud_rate: 115200

ld6002b:
  id: ld6002b_radar

sensor:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    target_count:
      name: Target Count
    target_1:
      x:
        name: Target-1 X
      y:
        name: Target-1 Y
      z:
        name: Target-1 Z
      dop_idx:
        name: Target-1 Doppler Index
    target_2:
      x:
        name: Target-2 X
      y:
        name: Target-2 Y
      z:
        name: Target-2 Z
      dop_idx:
        name: Target-2 Doppler Index
    target_3:
      x:
        name: Target-3 X
      y:
        name: Target-3 Y
      z:
        name: Target-3 Z
      dop_idx:
        name: Target-3 Doppler Index

binary_sensor:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    target_1:
      name: Target-1 Presence
    target_2:
      name: Target-2 Presence
    target_3:
      name: Target-3 Presence
```

### Full Configuration

```yaml
uart:
  id: uart_bus
  tx_pin: GPIOXX
  rx_pin: GPIOYY
  baud_rate: 115200

ld6002b:
  id: ld6002b_radar
  uart_id: uart_bus
  wakeup_pin: GPIOZZ
  wakeup_pulse_ms: 50ms
  auto_wake: true
  max_data_len: 1024
  throttle: 1s

binary_sensor:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    target:
      name: Presence
    target_1:
      name: Target-1 Presence
    target_2:
      name: Target-2 Presence
    target_3:
      name: Target-3 Presence
    area_0:
      name: Area-0 Presence
    area_1:
      name: Area-1 Presence
    area_2:
      name: Area-2 Presence
    area_3:
      name: Area-3 Presence

sensor:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    target_count:
      name: Target Count
    point_count:
      name: Point Count
    target_1:
      x:
        name: Target-1 X
      y:
        name: Target-1 Y
      z:
        name: Target-1 Z
      dop_idx:
        name: Target-1 Doppler Index
      cluster_id:
        name: Target-1 Cluster
    target_2:
      x:
        name: Target-2 X
      y:
        name: Target-2 Y
      z:
        name: Target-2 Z
      dop_idx:
        name: Target-2 Doppler Index
      cluster_id:
        name: Target-2 Cluster
    target_3:
      x:
        name: Target-3 X
      y:
        name: Target-3 Y
      z:
        name: Target-3 Z
      dop_idx:
        name: Target-3 Doppler Index
      cluster_id:
        name: Target-3 Cluster
    interference_area_0:
      x_min:
        name: Interference-0 X Min
      x_max:
        name: Interference-0 X Max
      y_min:
        name: Interference-0 Y Min
      y_max:
        name: Interference-0 Y Max
      z_min:
        name: Interference-0 Z Min
      z_max:
        name: Interference-0 Z Max
    interference_area_1:
      x_min:
        name: Interference-1 X Min
      x_max:
        name: Interference-1 X Max
      y_min:
        name: Interference-1 Y Min
      y_max:
        name: Interference-1 Y Max
      z_min:
        name: Interference-1 Z Min
      z_max:
        name: Interference-1 Z Max
    interference_area_2:
      x_min:
        name: Interference-2 X Min
      x_max:
        name: Interference-2 X Max
      y_min:
        name: Interference-2 Y Min
      y_max:
        name: Interference-2 Y Max
      z_min:
        name: Interference-2 Z Min
      z_max:
        name: Interference-2 Z Max
    interference_area_3:
      x_min:
        name: Interference-3 X Min
      x_max:
        name: Interference-3 X Max
      y_min:
        name: Interference-3 Y Min
      y_max:
        name: Interference-3 Y Max
      z_min:
        name: Interference-3 Z Min
      z_max:
        name: Interference-3 Z Max
    detection_area_0:
      x_min:
        name: Detection-0 X Min
      x_max:
        name: Detection-0 X Max
      y_min:
        name: Detection-0 Y Min
      y_max:
        name: Detection-0 Y Max
      z_min:
        name: Detection-0 Z Min
      z_max:
        name: Detection-0 Z Max
    detection_area_1:
      x_min:
        name: Detection-1 X Min
      x_max:
        name: Detection-1 X Max
      y_min:
        name: Detection-1 Y Min
      y_max:
        name: Detection-1 Y Max
      z_min:
        name: Detection-1 Z Min
      z_max:
        name: Detection-1 Z Max
    detection_area_2:
      x_min:
        name: Detection-2 X Min
      x_max:
        name: Detection-2 X Max
      y_min:
        name: Detection-2 Y Min
      y_max:
        name: Detection-2 Y Max
      z_min:
        name: Detection-2 Z Min
      z_max:
        name: Detection-2 Z Max
    detection_area_3:
      x_min:
        name: Detection-3 X Min
      x_max:
        name: Detection-3 X Max
      y_min:
        name: Detection-3 Y Min
      y_max:
        name: Detection-3 Y Max
      z_min:
        name: Detection-3 Z Min
      z_max:
        name: Detection-3 Z Max

text_sensor:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    work_mode:
      name: Work Mode
    ota_version:
      name: OTA Version

number:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    hold_delay:
      name: Hold Delay (s)
    z_min:
      name: Z Min (m)
    z_max:
      name: Z Max (m)
    low_power_sleep_time:
      name: Low Power Sleep (ms)
    area_config:
      x_min:
        name: Area X Min
      x_max:
        name: Area X Max
      y_min:
        name: Area Y Min
      y_max:
        name: Area Y Max
      z_min:
        name: Area Z Min
      z_max:
        name: Area Z Max

select:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    sensitivity:
      name: Sensitivity
    trigger_speed:
      name: Trigger Speed
    installation_mode:
      name: Installation Mode
    area_id:
      name: Area ID

switch:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    low_power:
      name: Low Power
    point_cloud:
      name: Point Cloud
    target_display:
      name: Target Display

button:
  - platform: ld6002b
    ld6002b_id: ld6002b_radar
    apply_area:
      name: Apply Area
    auto_interference:
      name: Auto Interference
    get_areas:
      name: Get Areas
    clear_interference:
      name: Clear Interference
    reset_detection_area:
      name: Reset Detection Area
    get_delay:
      name: Get Hold Delay
    get_sensitivity:
      name: Get Sensitivity
    get_trigger_speed:
      name: Get Trigger Speed
    get_z_range:
      name: Get Z Range
    get_installation:
      name: Get Installation
    get_low_power_mode:
      name: Get Low Power Mode
    get_low_power_sleep_time:
      name: Get Low Power Sleep Time
    reset_unattended:
      name: Reset Unattended
    wake:
      name: Wake
```

## See Also

- [HLK-LD6002B 3D Presence Module Communication Protocol V1.2](http://h.hlktech.com/download/HLK-LD6002B/1/HLK-LD6002B-3D%E5%AD%98%E5%9C%A8%E6%A8%A1%E7%BB%84%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AEV1.2.pdf)
- [LD2410 Sensor](/components/sensor/ld2410)
- [LD2420 24GHz mmWave Radar Sensor](/components/sensor/ld2420)
- [LD2450 Sensor](/components/sensor/ld2450)
- [RD-03D mmWave Radar](/components/sensor/rd03d)
- <APIRef text="ld6002b.h" path="ld6002b/ld6002b.h" />
