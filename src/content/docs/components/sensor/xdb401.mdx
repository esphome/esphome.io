---
description: "Instructions for setting up the XDB401 Pressure sensor."
title: "XIDIBEI XDB401 Pressure Sensor"
---

The XIDIBEI XDB401 is available with either I²C or analog output. This ESPHome platform supports the I²C variant; different models have different maximum pressure ranges and may report in Pa or bar.

<Figure
  src="/images/xdb401.jpg"
  alt=""
  caption="XDB401 Pressure Sensor. (Credit: [XIDIBEI](https://xdbsensor.com/product/xdb401-pro-ss316l-pressure-transducer-for-coffee-machine/), image cropped and compressed)"
  layout="constrained"
  width={300}
  height={300}
/>

To use the sensor, set up an [I²C Bus](/components/i2c) and connect the sensor to the specified pins.

```yaml
# Example configuration entry
# It uses a filter to change the unit
sensor:
  - platform: xdb401
    update_interval: 1s
    temperature: 
      id: temperature
      name: "Temperature"
      accuracy_decimals: 2
    pressure_range_bar: 10
    pressure:
      id: pressure_bar
      name: "Pressure"
      unit_of_measurement: "bar"
      accuracy_decimals: 2
      filters:
        - multiply: 0.00001   # Pa -> bar
```

## Configuration variables

- **temperature** (*Optional*): All options from [Sensor](/components/sensor).
- **pressure_range_bar** (*Optional*, int): Maximum pressure range of your sensor in bar (for example, `10` for a 0–10 bar sensor).
- **pressure** (*Optional*): All options from [Sensor](/components/sensor).
- **update_interval** (*Optional*, [Time](/guides/configuration-types#time)): The interval to check the sensor. Defaults to `60s`.

## See Also

- [esphome-pressure device](https://github.com/gcormier/esphome-pressure/)
- [Sensor Filters](/components/sensor#sensor-filters)
