---
description: "Understanding boards in ESPHome"
title: "Understanding boards in ESPHome"
---

import { Image } from 'astro:assets';
import i-dont-know-what-board-i-have-fig-1Img from './images/i-dont-know-what-board-i-have-fig-1.png';
import i-dont-know-what-board-i-have-fig-2Img from './images/i-dont-know-what-board-i-have-fig-2.png';
import i-dont-know-what-board-i-have-fig-3Img from './images/i-dont-know-what-board-i-have-fig-3.png';
import create-a-new-project-fig-1Img from './images/create-a-new-project-fig-1.png';
import board-variants-pill-listImg from './images/board-variants-pill-list.png';
import generic-boards-examplesImg from './images/generic-boards-examples.png';
import change-board-platform-dropdownImg from './images/change-board-platform-dropdown.png';
import yaml-board-variant-screenshotImg from './images/yaml-board-variant-screenshot.png';
import Figure from '@components/Figure.astro';

A board (circuit board) is the base for your ESPHome smart home device. It serves as the foundation for connecting electronic components. The main role of a board is to provide structure and support for the components. It also facilitates electrical connections between these components.

You can find out more basics about circuit boards generally on the [wikipedia page for ‘printed circuit board’](https://en.wikipedia.org/wiki/Printed_circuit_board).

Every board has a name it is known as, which is often printed directly on a part of the board. This name/board type can be in different places depending on the type/size of the board or be absent. If you cannot find the board name either on the board or in information about your board then you’ll need to either [connect your board](https://esphome.io/guides/physical_device_connection/) using an appropriate cable when building your project in ESPHome builder or choose a board variant to start building.

## How to identify your board

* Check each side of your board. You’ll be looking for `ESP` `BK` `RP` `RTL` `LN` or `nRF` and other numbers/letters after it. There are other boards that are supported which you can find out more about at [supported boards](https://esphome.io/components/esp32/#variants).

<Figure
  src={i-dont-know-what-board-i-have-fig-1Img}
  caption="Example shows two opposite sides of a board. Check all sides of the board to ensure you haven’t missed details."
  layout="constrained"
  alt=""
/>

* On your board there is text that tells you the name/variant. You may want to have a way to magnify very small text either a magnifying lens or a camera to take a photo and zoom in can help. 

<Figure
  src={i-dont-know-what-board-i-have-fig-2Img}
  caption="Example shows an ESP32-S2 variant. You may find dashes and different combinations of spacing on different boards."
  layout="constrained"
  alt=""
/>

* Alternatively, you can check details from where you purchased the board or any documents enclosed when your received it if you cannot find variant information on your board and connecting your board to auto-detect the details is not an option.

<Figure
  src={i-dont-know-what-board-i-have-fig-3Img}
  caption="Example shows a visual representation of an invoice/purchase receipt for a board with a variant name and an example of the UI to select that board type."
  layout="constrained"
  alt=""
/>

## I don’t have a board yet

If you want to start a configuration file for a project or explore what the possibilities are for building ESPHome smart home devices you can either start an empty configuration project which will create a blank YAML file which can be found under the advanced set up options.

<Image src={create-a-new-project-fig-1Img} layout="constrained" alt="A visual of the create new project user interface with help text that points to the link to expand advanced set up options and then empty configuration. The help text reads 'Expand the advanced options here and select empty configuration'" />

Alternatively you can choose any generic variant of any board (displayed below:ESP32, ESP32-S2, ESP32-S3, ESP32-C3, ESP32-C6, ESP32-HA, ESP8266, RP2040/RP2350, BK72xx, RTL87xx, LN882x, nRF52 and more board variants may be available) to get started and test out what’s possible and change the board using the interface or re-define in the YAML file at a later date. Be cautious when changing a board.

<Figure
  src={board-variants-pill-listImg}
  caption="Example above shows a list of some  generic variants. Other generic board may work with ESPHome."
  layout="constrained"
  alt=""
/>

<Figure
  src={generic-boards-examplesImg}
  caption="Example above shows the selection view of some generic variants when you start a new project in ESPHome. Use the search and filter function on the ‘select your board’ selection screen to find the board variant you’d like to test."
  layout="constrained"
  alt=""
/>

To change a board you selected at set-up, you first need to find and select the [Board name] Platform in the Device navigator menu. Then in the central section with input fields and places to type you should find a drop down that allows you to select another variant.

<Image src={change-board-platform-dropdownImg} layout="constrained" alt="A visual of the device navigator user interface showing the board platform selected and the associated drop down menu to select a new board platform variant" />

The YAML file can also be edited directly to change the platform variant. The YAML and the input fields reflect each other so changing one will also change the other.

<Image src={yaml-board-variant-screenshotImg} layout="constrained" alt="A visual of yaml code that changes when you select a new variant from the board platform drop down" />

## See Also

- [FAQ and Tips](https://esphome.io/guides/faq/)
- [variants](https://esphome.io/components/esp32/#variants)
