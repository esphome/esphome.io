# FAQ entries for the ESPHome Starter Kit page.
#
# Loaded by the `faq` content collection (see src/content.config.ts).
# Each entry under `items` has:
#   title:   Question shown in the FAQ heading / <summary>.
#   content: Answer body. Markdown is supported (rendered to HTML at build time).
#            Use the "|" block scalar to preserve newlines and indentation.
#
# TODO: Replace the placeholder questions and answers below with real content.

items:
  - title: Question 1 placeholder?
    content: |
      Placeholder answer for question 1. Markdown is supported here, including
      **bold**, *italics*, [links](https://esphome.io), and `inline code`.

  - title: Question 2 placeholder?
    content: |
      Placeholder answer for question 2.

      Multiple paragraphs are supported. Lists work too:

      - Item one
      - Item two
      - Item three

  - title: Question 3 placeholder?
    content: |
      Placeholder answer for question 3, demonstrating a fenced code block:

      ```yaml
      # example configuration
      sensor:
        - platform: example
          name: "Example Sensor"
      ```

  - title: Question 4 placeholder?
    content: |
      Placeholder answer for question 4, demonstrating a GitHub-style alert:

      > [!NOTE]
      > Replace this with real guidance once available.
